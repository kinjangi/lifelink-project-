services:
  # Backend API
  - type: web
    name: lifelink-backend
    env: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        value: mongodb+srv://akhilkrishnakondri_db_user:Gr3Z0FUZGwX6G2rI@cluster0.7dzw1je.mongodb.net/lifelink?retryWrites=true&w=majority
      - key: JWT_SECRET
        value: your-super-secret-jwt-key-change-this-in-production-12345
      - key: JWT_EXPIRE
        value: 30d
      - key: ML_API_URL
        value: https://lifelink-ml.onrender.com
      - key: ML_SERVICE_URL
        value: https://lifelink-ml.onrender.com
      - key: FRONTEND_URL
        value: https://akhilkrishnak25.github.io/lifelink

  # ML Service
  - type: web
    name: lifelink-ml
    env: python
    buildCommand: cd ml && pip install -r requirements.txt
    startCommand: cd ml && gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: PORT
        value: 10000
